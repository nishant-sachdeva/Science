import numpy as np 
import matplotlb.pyplot as plt 

import van_hove


def dynamic_structure_factor(set_of_frames):
	# so, we have the set of frames now

	# First, we'll write the main code in the jupyter notebook, then arrange it all here
	